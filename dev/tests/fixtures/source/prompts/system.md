# System Prompt

Load config from ~/.claude/prompts/defaults.md.
Use ./. claude/templates/ for project scaffolding.

Run /gsp:build after setup.
Use the AskUserQuestion tool for input.
Use the Skill tool to chain workflows.

Spawn the `gsp-project-builder` agent with context.
