# Stack init — React Native + NativeWind

Loaded on demand by `gsp-scaffold/SKILL.md` Step 3 when `tech_stack` is React Native + NativeWind. All commands run in `APP_PATH` (`cd {APP_PATH} && ...`).

```bash
npm install nativewind tailwindcss
npx @react-native-reusables/cli init
```
