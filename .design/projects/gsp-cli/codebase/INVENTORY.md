# Codebase Inventory

> Auto-generated by `/gsp:start` during codebase analysis.
> Downstream phases consume this instead of rescanning.

## Classification

**Codebase type:** existing
**Rationale:** Mature CLI tool (v0.4.1) with multi-runtime installer, 16 agents, 20 commands, completed 5-phase branding pipeline, and design token system.

## Tech Stack

| Layer | Value |
|-------|-------|
| Framework | Node.js CLI (no web framework) |
| Language | JavaScript (CommonJS, no TypeScript) |
| Styling | ANSI escape codes (terminal-native) |
| UI Kit | Custom terminal component system (brand-defined) |
| Package Manager | npm |
| Build Tool | none (direct Node.js execution) |

## Existing Components

| Component | Path | Props / Variants | Reusable? | Notes |
|-----------|------|------------------|-----------|-------|
| Splash art | bin/install.js | paint-splatter gradient | no | Inline in installer |
| Statusline | scripts/gsp-statusline.js | model, phase, prettiness, context | yes | Claude Code hook |
| Dispatcher | scripts/statusline-dispatcher.js | GSP/GSD routing | yes | Routes to correct statusline |

## Tokens & Theming

**Token source:** `.design/branding/get-shit-pretty/system/tokens.json`
**Format:** W3C Design Tokens Community Group JSON (v1.1.0), terminal-adapted with ANSI escape code values

| Category | Defined? | Details |
|----------|----------|---------|
| Colors | yes | Accent (burnt orange #FF6B35, 11 stops), text (3 levels), surface (5 levels), semantic (4), syntax (5). ANSI truecolor + 256 + 16 fallbacks. |
| Typography | yes | Terminal formatting: bold, dim, italic, underline, strikethrough, inverse, reset |
| Spacing | yes | Vertical (lines), horizontal (columns), box padding |
| Radii | no | N/A (terminal) |
| Shadows | no | N/A (terminal) |
| Dark mode | yes | Dark-first (terminal default). Surface palette is black-based. |

## Architecture Patterns

| Pattern | Value |
|---------|-------|
| Component style | Markdown-defined terminal components (design specs, not code) |
| State management | File-based (STATE.md, config.json per brand/project) |
| Data fetching | Filesystem reads, stdin JSON parsing for statusline |
| Routing | Command-based (`/gsp:{command}` pattern, 20 commands) |
| File organization | Type-based: agents/, commands/gsp/, prompts/, templates/, references/, scripts/, bin/ |

## Conventions

| Convention | Value |
|------------|-------|
| Naming | kebab-case files, numbered prefixes for prompts |
| Exports | npm bin entry point (bin/install.js) |
| Styling approach | ANSI escape codes in JS, design tokens as JSON |
| Import aliases | None (CommonJS require with relative paths) |

## Key Paths

| Path | Purpose |
|------|---------|
| bin/install.js | Multi-runtime installer (splash art, file copying/symlinking) |
| scripts/gsp-statusline.js | Claude Code statusline hook |
| scripts/statusline-dispatcher.js | Routes to GSP or GSD statusline |
| .design/branding/get-shit-pretty/system/tokens.json | Design token source of truth |
| .design/branding/get-shit-pretty/system/components/ | 15 terminal component specs |
